<%= form_with model: [group, comment] do |f| %>
  <%= render 'layouts/error_messages', model: f.object %>
  <%= f.text_area :body, rows: 5, class: "form-control", placeholder: "ここからコメントを投稿" %>
  <%= f.submit class: "btn btn-primary my-4" %>
<% end %>